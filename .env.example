# Prism Catalyst – copy to .env and fill values
# API / Auth
NODE_ENV=development
API_PORT=3000
JWT_SECRET=

# Supabase PostgreSQL – use connection string from Supabase dashboard (Settings → Database)
# Direct: postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
DATABASE_URL=
# Or individual vars (e.g. for local Postgres or custom host)
# POSTGRES_HOST=db.xxx.supabase.co
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=
# POSTGRES_DB=postgres

# Market data (Phase 2) – get key at https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=
# Ingestion: comma-separated symbols; delay between API calls (free tier ~5/min)
# INGEST_SYMBOLS=AAPL,MSFT,GOOGL
# INGEST_QUOTE_DELAY_SECONDS=12

# Redis (Phase 2) – quote cache; API reads from here. Docker: redis on 6380.
REDIS_URL=
# Optional: TTL for cached quotes in data-pipeline (seconds; default 300)
# QUOTE_CACHE_TTL_SECONDS=300

# Optional (use these ports when using infra/docker compose to avoid local conflicts)
# MONGODB_URI=mongodb://localhost:27018/prism
# FRONTEND_URL=http://localhost:5173
